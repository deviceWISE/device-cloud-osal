~~ Manually Building with GCC ~~

*POSIX*
 1. Build objects:
 gcc -c -fpic -Wall -Werror os.c os/os_posix.c -D_GNU_SOURCE=1 -l dl -l pthread -l uuid
 
 2. Create SharedObject library:
 gcc --shared -o osal.so *.o

*Windows*
 N/A

~~ Building with CMake ~~

*POSIX*
 1. Create a build folder and cd into it
 2. Run `cmake ..`
 3. Run `make`

*Windows*
 1. Create a build folder and cd into it
 2. Run `cmake ..`
 3. Open the newly generated `Project.sln` in Visual Studio
 4. Run the Build action on the `BUILD ALL` component